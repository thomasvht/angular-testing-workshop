import {HeroService} from './hero';

describe('UserService', () => {

    const heroList: Array<string> = ['Batman', 'Superman'];

    it('should return a list of heroes', () => {
        // Arrange

        // Act

        // Assert
    });

    it('should return a list of heroes 2', () => {
        // Arrange

        // Act

        // Assert
    });
});
